{
  "name": "who-is-the-impostor",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "npm run test:unit",
    "test:unit": "node --test tests/catalog.test.js tests/domain.test.js tests/engine.test.js tests/database-reference.test.js tests/word-reminder.test.js",
    "test:rules": "firebase emulators:exec --only database \"node --test tests/database.rules.test.js\"",
    "test:spark-flow": "firebase emulators:exec --only database \"node --test tests/spark-flow.integration.test.js\"",
    "test:spark-voting": "firebase emulators:exec --only database \"node --test tests/spark-voting.integration.test.js\"",
    "check": "npm run lint && npm run test && npm run build",
    "verify:spark": "npm run check && npm run test:rules && npm run test:spark-flow && npm run test:spark-voting",
    "emulators": "firebase emulators:start",
    "deploy": "npm run verify:spark && firebase deploy --only database,hosting"
  },
  "dependencies": {
    "firebase": "^12.17.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@firebase/rules-unit-testing": "^5.0.1",
    "eslint": "^10.8.1",
    "firebase-tools": "^15.26.0",
    "globals": "^17.0.0",
    "vite": "^8.2.1"
  }
}
